header:
  inherits: ../../yaml-library/raw-program.yaml  => raw-program 
  name: window-program

configuration:
  agent-or-label: $(overridable) default
  context-name: $(overridable) default

action:
  environment-variables:
    - (work-folder) home/workfolder
  api: $(overridable) default
  